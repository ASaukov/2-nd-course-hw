        // 1 //
let smallNumber = (a, b) => {
        return a <= b ? a : b;
}
console.log(smallNumber(6, 4));

        // 2 //
let parity = x => {
        return x % 2 ===0 ? 'Число четное' : 'Число не четное';
}
console.log(parity(10));

